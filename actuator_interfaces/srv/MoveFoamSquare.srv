# Side length of the square in degrees of string travel
float32 side_length
# Pause between each side in seconds (0.0 = no pause)
float32 step_delay
# Optional CSV label override (empty = auto-generate)
string label
---
bool success
string message
